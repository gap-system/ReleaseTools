2017-02-05:
 * Fix copying htm directory into gh-pages branch (for packages using gapmacro)
 * Adjust HTML links in gh-pages to point to GAP manuals on the GAP website

2017-02-02:
 * Fix problem where user interactive InstallAtExit handlers could get
   the `release` script stuck
 * After a successful run of the release script, display a reminder to run the
   online package validator, and show the PackageInfo.g URL to help with that.
